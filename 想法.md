
# **一个受控制的agent = 充足的信息背景 + 按需加载的执行规范**

先规范场景，每个场景启用不同的agent，不同的agent会使用不同的skills。当前是什么场景，这个由用户的prompt明确，agent看到关键词，启用不同的agent身份。这里的agent身份，你可以理解成对agent做的“人设”（我们经常让agent做人设，比如“你现在是个有5年Java开发经验的工程师、你现在是个自身DBA，然后你要帮我干啥干啥”）。

需要明确：需求开发只是研发日常工作的一部分，我们需要不同的人设，来执行不同的工作内容

四类大角色：1-需求分析角色；2-需求影响确认角色；3-代码review角色；4-线上bug排查角色；

就需求开发来讲：我们会用到前三个角色。

需求分析阶段一个角色、技术方案设计一个角色、编码一个角色、codereview一个角色、测试一个角色、上下评估一个角色。

一个角色一个".md"类型的文档，文档里有执行步骤。当前服务需要注意的事项。

## 设计思路：

细化agent角色：一个OR多个步骤派生一个子agent（角色）做垂直领域的事情。需求分析就做需求分析，写代码就做写代码

按需加载约束：不是所有内容都一股脑加载，执行到某个角色的某个步骤的时候，按需要加载skills或者知识。

## 1 信息背景：

1. 项目架构信息（就是纯代码结构，这样agent开发的时候不需要每次都扫一遍我们的系统了，节省了上下文空间），包括：1 本系统的架构与能力；2 外部依赖的知识（这部分按需加载，需要的时候才去云端找，减少上下文空间占用）
    
2. 项目知识库，（业务能力和接口能力）包括：服务能力是啥、接口有哪些。
    

### 1.1 服务架构文档

1. 项目架构概览。按照client、service、dao、web这些分层划分，明确内层的功能职责和能力。
    
2. 分层调用关系。分层之间的依赖关系，核心业务。
    
3. 核心链路。请求入口：哪些RPC和HTTP接口、MQ入口、crane；持久化层：Redis、DB、ES
    
4. 核心数据模型。
    
5. 外部RPC依赖。所有下游appkey，以及gateway，依赖等级等。
    
6. 基于第5部分，需要有各个服务的服务知识库。后续新接一些接口，直接找知识库即可。
    
7. 项目开发规范。业务沉淀知识。
    
8. 需求迭代记录。同步更新到知识库中。
    

### 1.2 服务知识库（同步到云端）

整体业务能力 + 详细的接口能力（主要是接口能力，因为是给AI看的）

1. 当前服务能力。整体服务的业务能力是啥，这个和本地版本可以一样。
    
2. 接口能力包括两部分：业务视角（能力是啥） + 协议规范（其他人怎么用）
    
    1. 核心业务能力：业务视角描述接口能力是什么。
        
    2. 接口交互协议：当前服务接口协议，包括入参规范（set某个字段后，才有返回对应的信息），请求量限制（例如一次查200个），返回值边界（超过某个时间、超过某个条件的数据不会返回）。
        
3. 最新的client包版本，如何引入依赖的demo代码（这样上游服务直接就能知道怎么用，然后结合他自己的rpc规范，自动拉取需要的外部依赖）
    

## 2 执行规范有：

1. claude.md文档，核心规约，规范了agent做需求的时候，必须串行执行“spec6步”。
    
2. 开发standard，详细规范了整个workflow的流程。
    
    1. 执行workflow的过程时，会排成多个不同角色的子agent，这些agent执行workflow步骤的时候，按需加载skills和规范。
        

### 2.1 claude.md（agent核心规范）

指导agent接到命令后怎么做

1. 分支命名规范
    
2. 执行必须走工作流闭环，新需求必须：走上面的6步，不能跳转
    
3. 需求开发的规范：交代执行对应6步，有一个映射表，说了哪些关键字，自动映射到对应的步骤上。但是信息求必须从 spec 开始，顺序执行。
    
4. bug排查的规范：待建设
    

### 2.2 开发standard

1. workflow流程是什么。每一步扮演什么角色，每一个角色输出什么，执行下一步的条件是什么（比如必须人工确认才能执行下一步）
    
2. agent必须遵守的规范
    
3. 可以自助执行的操作
    
4. 必须等待人工确认的内容
    
5. 跨服务规范追加
    

### 2.3 bug排查standard

xxx

### 2.4 skills组成

两部分：公共部分 + 服务专属部分

1. 公共部分一些skills
    
    1. 需求规划，需求拆解
        
        1. 拆解规范：需求拆解后自动判断复杂度，选择不同的模板。比如：配置变动，字段扩展，接口扩展，链路变动，跨服务改动，复杂度逐渐增加。按照不同的模板复杂度，写不同的spec
            
        2. 如果需求过大，或者涉及多个互相独立的子系统，不能直接写spec，必须做拆解，列出子项目的依赖关系，列出交付顺序（先做哪个）。核心是：一个spec只能描述一个可独立交付的改动单元。
            
    2. 任务拆分。
        
2. 服务专属的skills
    

# spec步骤

1. spec
    
    1. 文件本身内容不多，只是描述了如何**创建一个结构化的规格说明**。有两个重点：1-指明需要调用sdd-skills的命令，真正的harness规范都在这里。2-指明spec文档根据复杂度划分
        
    2. 那这个skills做了什么？对齐，对齐需求改动点，对齐边界。如果是跨服务的，需要load对方的知识库，对齐上下游影响。
        
2. plan：方案。有明确的实现标准，比如通过一些测试。
    
3. build（coding）：开发写代码。
    
4. review
    
5. test
    
6. online
    


workflow
```
Phase 0: 需求澄清
   使用: superpowers:brainstorming（苏格拉底式问答，探索上下文，提出 2-3 方案）
   角色: Architect 主导，Developer 澄清技术细节
         team-lead 创建 task_plan.md, findings.md, progress.md（三文件规划）
   产出: design.md + 三文件 → 人工确认 ①

Phase 1: 实现计划
   使用: superpowers:writing-plans（可选：内部知识库搜索）
   角色: Developer 写 developer_plan.md（技术方案 + TDD 任务分解）
         Architect + Tester 并行评审（最多 3 轮，各维度评分）
   产出: developer_plan.md → 人工确认 ②

Phase 2: 测试用例生成
   角色: Tester 调用 e2e-test-analysis skill 生成，Developer 打分评审（3 维度，总分 ≥ 7 通过），达成共识
   产出: test_case.md

Phase 3: 实施编码
   使用: superpowers:test-driven-development（RED-GREEN-REFACTOR 循环）
   角色: Developer TDD 驱动，按 task_plan.md 并行认领任务
   产出: 代码 + 测试 + 更新 progress.md

Phase 4: Code Review
   使用: superpowers:requesting-code-review
   Layer 1: 规范符合性审查（Architect，100% 符合通过）→ spec_compliance_review.md
   Layer 2: 代码质量审查（4 维度打分，总分 ≥ 8 通过）→ code_review.md

Phase 5: 提交与 PR 创建
   角色: Developer 执行 git commit + push + 创建 PR（可选：Meituan PR）

Phase 6: Cargo 测试环境部署
   角色: Developer 执行 cargo-cli 预检查、部署与状态轮询
   产出: cargo_deploy.md

Phase 7: 集成测试
   角色: Tester 在 Cargo 泳道环境执行集成测试（4 维度打分，总分 ≥ 8 通过），失败则 Developer 修复
   产出: test_report.md
   ↓
完成（team-lead 解散团队）

Phase B: BUG 修复（可在任意时机触发）
   触发: 对话中包含 "bug"、"有问题"、"报错"、"fix" 等关键词
   流程: Developer 快速评估 → 三方路由决策 → 路由到 Phase 1/3/4 继续
   产出: bug_report.md
```



# 不符合预期的点 OR 遇到的问题

1. 异常拦截校验是中文，不是英文。M端，如果是抛异常的场景，需要确认是否展示在页面，如果展示，应该做国际化处理。
    
2. DTO转换为Thrift的过程，是否能使用Mapper处理，或者保证一个服务内部的格式统一？
    
3. 日志打印，反序列化的方式，使用统一的Tool
    
4. 操作数据库表，是否影响主流程，抛异常是否需要感知。MQ消费逻辑？M端查询逻辑？C端查询逻辑？
    
5. batchInsert后，主键回填错误，需要删除userGenerationKey这个参数
    
6. 批量RPC，超过100会有问题。